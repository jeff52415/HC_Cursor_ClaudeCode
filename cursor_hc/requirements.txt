# Installation:
# Using uv (recommended): uv add torch
# Using pip: pip install -r requirements.txt

# Core dependencies
torch>=2.0.0

# Optional: for testing and development
pytest>=7.0.0
numpy>=1.20.0
